<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>{{ $coupon->exists ? 'Edit coupon' : 'Create coupon' }}</title>@vite(['resources/css/app.css', 'resources/js/app.js'])</head>
<body class="admin-shell"><div class="shell py-8"><a href="{{ route('admin.coupons.index') }}" class="text-sm text-sky-700">← Back to coupons</a><form method="POST" action="{{ $coupon->exists ? route('admin.coupons.update', $coupon) : route('admin.coupons.store') }}" class="mt-6 glass-panel p-6">@csrf @if($coupon->exists) @method('PUT') @endif<div class="grid gap-4 md:grid-cols-2"><input name="code" value="{{ old('code', $coupon->code) }}" placeholder="Code" class="rounded-2xl border border-sky-100 px-4 py-3 text-slate-900"><input name="description" value="{{ old('description', $coupon->description) }}" placeholder="Description" class="rounded-2xl border border-sky-100 px-4 py-3 text-slate-900"><select name="type" class="rounded-2xl border border-sky-100 px-4 py-3 text-slate-900"><option value="fixed" @selected(old('type', $coupon->type) === 'fixed')>Fixed</option><option value="percent" @selected(old('type', $coupon->type) === 'percent')>Percent</option></select><input name="value" value="{{ old('value', $coupon->value) }}" placeholder="Value" class="rounded-2xl border border-sky-100 px-4 py-3 text-slate-900"><input name="minimum_amount" value="{{ old('minimum_amount', $coupon->minimum_amount) }}" placeholder="Minimum amount" class="rounded-2xl border border-sky-100 px-4 py-3 text-slate-900"><input name="usage_limit" value="{{ old('usage_limit', $coupon->usage_limit) }}" placeholder="Usage limit" class="rounded-2xl border border-sky-100 px-4 py-3 text-slate-900"><input type="datetime-local" name="starts_at" value="{{ old('starts_at', optional($coupon->starts_at)->format('Y-m-d\TH:i')) }}" class="rounded-2xl border border-sky-100 px-4 py-3 text-slate-900"><input type="datetime-local" name="ends_at" value="{{ old('ends_at', optional($coupon->ends_at)->format('Y-m-d\TH:i')) }}" class="rounded-2xl border border-sky-100 px-4 py-3 text-slate-900"><label class="flex items-center gap-3 text-slate-700"><input type="checkbox" name="is_active" value="1" @checked(old('is_active', $coupon->is_active ?? true))> Active</label></div><button class="btn-primary mt-6">{{ $coupon->exists ? 'Update coupon' : 'Create coupon' }}</button></form></div></body></html>
