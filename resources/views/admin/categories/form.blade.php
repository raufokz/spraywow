<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>{{ $category->exists ? 'Edit category' : 'Create category' }}</title>@vite(['resources/css/app.css', 'resources/js/app.js'])</head>
<body class="admin-shell"><div class="shell py-8"><a href="{{ route('admin.categories.index') }}" class="text-sm text-sky-700">← Back to categories</a><form method="POST" action="{{ $category->exists ? route('admin.categories.update', $category) : route('admin.categories.store') }}" class="mt-6 glass-panel p-6">@csrf @if($category->exists) @method('PUT') @endif<div class="grid gap-4 md:grid-cols-2"><input name="name" value="{{ old('name', $category->name) }}" placeholder="Name" class="rounded-2xl border border-sky-100 px-4 py-3 text-slate-900"><input name="slug" value="{{ old('slug', $category->slug) }}" placeholder="Slug" class="rounded-2xl border border-sky-100 px-4 py-3 text-slate-900"><input name="accent_color" value="{{ old('accent_color', $category->accent_color) }}" placeholder="#38bdf8" class="rounded-2xl border border-sky-100 px-4 py-3 text-slate-900"><input name="hero_copy" value="{{ old('hero_copy', $category->hero_copy) }}" placeholder="Hero copy" class="rounded-2xl border border-sky-100 px-4 py-3 text-slate-900"><textarea name="description" rows="5" class="rounded-2xl border border-sky-100 px-4 py-3 text-slate-900 md:col-span-2">{{ old('description', $category->description) }}</textarea></div><button class="btn-primary mt-6">{{ $category->exists ? 'Update category' : 'Create category' }}</button></form></div></body></html>
